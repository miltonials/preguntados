@model IEnumerable<preguntados.Models.Resultado>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RespuestaElegida)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EsCorrecta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaHoraRespuesta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Jugador)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pregunta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RespuestaElegida)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EsCorrecta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaHoraRespuesta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Jugador.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pregunta.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
